<?php
/** @var \Magento\Framework\Escaper $escaper */
$count = count($block->getConfig()->getIgnoredHashes());

if ($count): ?>
    <p>
        <?= $escaper->escapeHtml(__('The above list has been filtered to ignore %1 errors.', $count)) ?>
    </p>

    <form method="post" action="<?= $escaper->escapeUrl($block->getUrl('*/*/resetIgnore')); ?>">
        <input type="hidden" name="form_key" value="<?= $block->getFormKey() ?>" />

        <button type="submit">
            <?= $escaper->escapeHtml(__('Reset ignore list to show all errors')) ?>
        </button>
    </form>
<?php endif;
